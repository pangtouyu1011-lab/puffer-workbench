# styles.css 最终职责审计

审计阶段：Legacy 清理 5.4.3
审计日期：2026-08-14
审计方式：只读扫描，不修改 CSS、HTML、JS、Worker、同步逻辑或数据结构。

## 结论

本轮拆分后，`styles.css` 已经基本收敛为公共基础层、旧数据/功能兼容层和公共弹窗通知样式。旧 Dashboard、旧训练页和旧导航的主体样式已经分别迁移到：

- `legacy-dashboard.css`
- `legacy-training.css`
- `legacy-navigation.css`

当前不建议继续大规模拆分 `styles.css`。它仍包含旧版数据页、素材库、留言、心愿、音乐浮窗以及公共弹窗/Toast 的兼容样式，这些仍可能被 `app.js` 或旧 DOM 使用。

## 1. `styles.css` 当前职责

### 全局基础层

- `:root` 颜色、字体、阴影、圆角和基础变量；
- `html`、`body`、背景泡泡和背景装饰；
- 通用页面宽度、滚动和安全区处理；
- 基础按钮、输入框、选择框、徽章和文字辅助类。

### 旧兼容布局层

- `.app`、`.content`、`.page` 等旧页面容器；
- 旧版页面仍可能使用的公共 `card`、`card-head`、`page-head`、`page-title`；
- 待办、素材库、留言、心愿和音乐浮窗等尚未迁移的旧功能样式。

### 公共交互层

- `.modal-mask`、`.modal`、`.modal-head`、`.modal-body`、`.modal-foot`；
- `.toast`、消息徽标和通知开关；
- 公共点击反馈、减少动态效果和基础响应式规则。

### 设置与同步兼容层

- 设置弹窗、同步卡片、房间信息、导入导出和危险操作区域；
- 这些规则仍与旧 `app.js` 兼容接口有关，不建议现在删除。

## 2. Legacy 专属残留检查

### 已完成迁移

- Dashboard 统计卡、旧喝水卡、Dashboard 网格和快捷入口：已进入 `legacy-dashboard.css`；
- 训练计划、训练日记、训练卡片、训练周视图和训练响应式规则：已进入 `legacy-training.css`；
- 旧侧边栏、旧导航项、旧移动端底部导航和旧导航响应式规则：已进入 `legacy-navigation.css`。

### 当前仍可见的旧兼容痕迹

- `.app` 的旧布局网格仍保留，因为旧 HTML 和 `app.js` 兼容路径仍存在；
- `#legacy-ui` 相关的少量保护规则仍保留，用于避免旧 DOM 样式误影响；
- 公共点击反馈选择器中仍包含 `.nav-item`、`.bn-item`，这是兼容触摸反馈，不是导航主体样式；
- 旧功能的待办、素材、留言、心愿和音乐浮窗样式仍在 `styles.css`，目前不能判定为无用。

结论：没有发现需要立即删除的完整旧模块样式块。

## 3. 响应式规则检查

剩余响应式规则主要用于：

- 公共旧页面容器和内容宽度保护；
- 设置、弹窗、素材库、留言和音乐浮窗；
- 公共卡片、按钮和移动端安全区；
- Life 页面兼容入口附近的最小宽度和溢出保护。

旧 Dashboard、训练和导航的主体响应式规则已经随各自文件迁移。剩余规则不建议为了文件整洁继续搬动，因为其中部分选择器属于公共组件或兼容页面。

## 4. 重复与覆盖检查

- `life.css`、`life-ritual.css`、`life-complete-state.css` 和各 `legacy-*.css` 之间未发现需要立即合并的完整同名模块；
- `styles.css` 中的 `.card`、`.page-head`、`.settings-*` 等规则属于旧兼容/公共层，不能仅因名字普通就删除；
- 部分设置和通用按钮规则存在历史重复定义，但当前级联结果是生产视觉的一部分，建议另开“公共样式收敛”任务处理；
- `life-dashboard.css` 名称包含 dashboard，但审计确认其实际规则属于当前 Life 页面，不应与旧 Dashboard 混淆或删除。

## 5. 新版 Life 页面污染风险

当前风险为低到中：

- 旧 DOM 已通过 `hidden` 隔离；
- `app.js` 已有 Life 模式启动边界；
- 旧 Dashboard、训练和导航主体选择器已经移入独立兼容文件；
- `styles.css` 仍保留少量公共旧选择器，理论上可能影响共享类名，但未发现新的高风险 `width:100vw` 或未约束的旧主布局规则。

重点保留检查：`.card`、`.page`、`.modal`、`.toast`、`.app` 和公共响应式规则。

## 6. 可安全删除候选

本轮不建议直接删除文件或大块规则。未来只有在以下条件同时满足时，才可逐项删除：

1. 对应旧 DOM 已完全移除；
2. `app.js` 不再调用对应功能；
3. 导入导出、同步和历史数据兼容不再依赖；
4. HTML、JS、CSS 全局搜索确认无引用；
5. 手机 PWA 和桌面端回归测试通过。

当前可标记但不删除的候选：旧 `.app` 布局、旧页面公共容器、旧音乐浮窗兼容样式和旧训练数据展示兼容路径。

## 7. 不建议删除的公共规则

- `:root`、`html`、`body`、背景与安全区规则；
- `.card`、`.card-head`、`.pixel-btn`、`.pill`、`.modal`、`.toast`；
- 设置和同步相关样式；
- 公共触摸反馈、减少动态效果和溢出保护；
- 仍被旧数据迁移、导入导出或兼容 API 使用的页面规则。

## 8. 是否建议继续拆分

结论：**暂不建议继续拆分 `styles.css`。**

本轮目标已经达到：新 Life 样式有独立来源，旧 Dashboard/训练/导航有独立兼容层，`styles.css` 保留公共基础与必要兼容。继续拆分的收益已经低于误伤旧功能和生产级联关系的风险。

建议下一步：

1. 统一提交本轮 Legacy CSS 拆分；
2. 在本地做 iPhone 尺寸、桌面端和旧 DOM 隔离回归；
3. 暂停 CSS 重构，回到产品功能和真实体验优化；
4. 未来若清理旧功能，按“先移除引用、再删除 CSS”的单功能提交方式推进。

## 9. 本轮验证

- `styles.css` 花括号：566 / 566；
- `legacy-dashboard.css` 花括号：46 / 46；
- `legacy-training.css` 花括号：46 / 46；
- `legacy-navigation.css` 花括号：27 / 27；
- `node --check app.js`：通过；
- `node --check life.js`：通过；
- `git diff --check`：通过；
- 未部署。
