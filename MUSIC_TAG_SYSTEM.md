# 音乐标签体系设计

## 目标

为下一阶段推荐算法升级建立稳定、可解释、可渐进扩展的歌曲标签体系。

本阶段只定义数据规范，不修改现有推荐算法、页面、同步、Worker 或歌曲推荐结果。

## 一、当前歌曲数据结构分析

当前歌曲主要使用以下字段：

```js
{
  title: '歌曲名',
  artist: '歌手',
  id: '平台歌曲 ID',
  tags: ['morning', 'rain', 'soft'],
  source: '来源歌单',
  url: '平台链接'
}
```

现有 `tags` 同时承担了几种不同职责：

- 时间段：`morning`、`noon`、`night`；
- 天气：`rain`、`sun`、`cloud`；
- 情绪或能量：`happy`、`soft`、`tired`、`slow`；
- 来源或偏好风格：`indie`、`rap`、`experimental`；
- 星期场景：`friday`、`weekend`、`workweek`。

这种方式可以支持当前规则，但含义不够清晰。后续推荐算法需要区分“歌曲本身的属性”和“当天上下文”，因此建议保留旧 `tags` 兼容，同时逐步增加结构化标签。

## 二、推荐所需标签设计

### 1. genre：音乐类型与风格

描述歌曲本身属于什么音乐范围。

建议使用少量稳定标签：

```text
华语、欧美、日系、独立音乐、独立乐队、流行、民谣、摇滚、说唱、R&B、电子、影视原声
```

不要为每首歌创建过细的独有标签，例如“某某歌手某年某张专辑风格”。

### 2. mood：情绪氛围

描述歌曲带来的主要情绪，不代表对用户当前情绪做诊断。

建议使用：

```text
开心、轻松、浪漫、治愈、安静、怀念、温柔、热烈、孤独、释放、鼓励
```

一首歌建议选择 1～3 个情绪标签。

### 3. scene：适合使用的生活场景

描述什么时候听比较自然。

建议使用：

```text
早晨、通勤、工作、学习、散步、约会、旅行、做饭、夜晚、睡前、下雨天、周末
```

场景是推荐上下文，不代表歌曲只能在这些场景播放。

### 4. energy：能量等级

使用固定枚举，避免自由文本：

```text
low       安静、舒缓、适合慢下来
medium    有节奏但不强烈，适合日常陪伴
high      明快、热烈，适合出门、运动或庆祝
```

## 三、标签字段设计

建议的新结构如下：

```js
{
  title: '爱人错过',
  artist: '告五人',
  id: '535739351',
  tags: ['morning', 'sun', 'happy'],
  profile: {
    genre: ['华语', '独立乐队'],
    mood: ['青春', '浪漫', '轻松'],
    scene: ['散步', '约会', '周末'],
    energy: 'medium'
  },
  source: '相似推荐',
  url: '...'
}
```

### 兼容原则

- 现有 `tags` 暂时保留；
- 新 `profile` 缺失时，推荐算法继续使用旧 `tags`；
- 不改歌曲 ID、来源、平台链接；
- 不用标签文字作为同步或状态主键；
- 标签只属于歌曲知识层，不进入情侣房间同步；
- 一首歌最多保留少量高置信度标签，宁缺毋滥。

## 四、未来推荐算法如何使用标签

未来算法可将歌曲标签与当前上下文分开计算：

```text
最终分数 =
个人偏好匹配       35%
歌曲标签匹配       25%
天气匹配           15%
时间场景匹配       10%
星期与当天氛围     10%
新鲜度              5%
```

例如：

```text
当前：周五晚上、晴天、适合散步
偏好：华语、独立乐队、浪漫

优先寻找：
genre 里有华语或独立乐队
mood 里有浪漫或轻松
scene 里有夜晚或散步
energy 为 medium
```

### 推荐解释

标签可以生成简短、真实的推荐理由：

```text
今天是周五晚上，天气不错，
这首华语乐队作品适合一起散步时听。
```

解释只使用实际标签和上下文，不虚构用户没有提供的心理状态。

## 五、示例歌曲标签

以下是第一批示例。它们是设计示例，不代表本阶段立即修改歌曲库。

### 1. 爱人错过 · 告五人

```js
profile: {
  genre: ['华语', '独立乐队'],
  mood: ['青春', '浪漫', '轻松'],
  scene: ['散步', '约会', '周末'],
  energy: 'medium'
}
```

### 2. 特别的人 · 方大同

```js
profile: {
  genre: ['华语', 'R&B'],
  mood: ['温柔', '浪漫', '治愈'],
  scene: ['夜晚', '约会', '睡前'],
  energy: 'low'
}
```

### 3. 爱在西元前 · 周杰伦

```js
profile: {
  genre: ['华语', '流行'],
  mood: ['怀念', '浪漫', '叙事'],
  scene: ['通勤', '散步', '周末'],
  energy: 'medium'
}
```

### 4. 心的距离 · 陈奕迅

```js
profile: {
  genre: ['华语', '流行'],
  mood: ['怀念', '安静', '孤独'],
  scene: ['夜晚', '下雨天', '睡前'],
  energy: 'low'
}
```

### 5. 凄美地 · 郭顶

```js
profile: {
  genre: ['华语', '独立音乐'],
  mood: ['安静', '怀念', '治愈'],
  scene: ['夜晚', '下雨天', '散步'],
  energy: 'low'
}
```

### 6. Pink + White · Frank Ocean

```js
profile: {
  genre: ['欧美', 'R&B'],
  mood: ['温柔', '轻松', '浪漫'],
  scene: ['早晨', '旅行', '周末'],
  energy: 'medium'
}
```

### 7. Best Part · Daniel Caesar feat. H.E.R.

```js
profile: {
  genre: ['欧美', 'R&B'],
  mood: ['浪漫', '温柔', '治愈'],
  scene: ['约会', '夜晚', '睡前'],
  energy: 'low'
}
```

### 8. Snooze · SZA

```js
profile: {
  genre: ['欧美', 'R&B'],
  mood: ['温柔', '浪漫', '轻松'],
  scene: ['夜晚', '约会', '睡前'],
  energy: 'low'
}
```

### 9. 旅行中忘记 · 旅行团乐队

```js
profile: {
  genre: ['华语', '独立乐队'],
  mood: ['轻松', '开心', '释放'],
  scene: ['旅行', '周末', '出门'],
  energy: 'high'
}
```

### 10. Calm Down · Rema & Selena Gomez

```js
profile: {
  genre: ['欧美', '流行'],
  mood: ['开心', '热烈', '轻松'],
  scene: ['下午', '周末', '出门'],
  energy: 'high'
}
```

## 六、80 首现有歌曲的迁移方案

不要一次性给所有歌曲打满标签，建议分三批：

### 第一批：高频与核心歌曲，约 20 首

优先处理：

- 当前经常推荐的歌曲；
- 两个人歌单里的代表歌曲；
- 已经有明确歌手和平台 ID 的歌曲；
- 用户实际点击或反馈过的歌曲。

要求每首至少具备：

- 1～2 个 `genre`；
- 1～3 个 `mood`；
- 1～3 个 `scene`；
- 1 个 `energy`。

### 第二批：剩余已有歌曲，约 40 首

根据歌手、歌曲气质和现有旧 `tags` 迁移。无法确认的属性不强行猜测，可以先只保留低置信度的场景标签。

### 第三批：相似推荐歌曲，约 20 首

重点标注风格和场景，避免所有相似推荐都被打成同一种“夜晚、下雨、伤感”。应保留不同能量和不同语言风格，才能真正扩大推荐范围。

### 迁移安全要求

- 不删除旧 `tags`；
- 不改变 `source`；
- 不改变歌曲 URL；
- 不改变歌曲 ID；
- 新标签缺失时使用旧规则回退；
- 标签无法确认时不作为强过滤条件；
- 不因为补标签而改变当前版本推荐结果。

## 七、歌曲库扩充建议

### 阶段一：80 → 150～300 首

先扩充到足以降低短期重复的规模，并确保每首歌有基本标签。

建议比例：

```text
现有个人歌单       40%
相似风格歌曲       35%
不同语言与风格     15%
季节或场景歌曲     10%
```

### 阶段二：300 → 1000 首

只有当标签质量、排除逻辑和历史机制稳定后，再扩大歌曲池。否则歌曲越多，错误推荐也越多，排查会更困难。

### 不能只追求数量

每首新增歌曲至少需要：

- 可访问的平台链接；
- 正确的歌名和歌手；
- 来源标记；
- 基础风格和情绪标签；
- 明确的重复键。

## 八、标签维护规则

- 标签使用固定小写枚举或固定中文词，不混用同义词；
- 不使用“可能”“大概”“超级好听”等不可计算标签；
- 每首歌的主要标签不超过 10 个；
- `energy` 只能是 `low`、`medium`、`high`；
- 标签修改只影响未来推荐，不修改历史推荐记录；
- 历史记录保存歌曲键和日期，不复制完整歌曲对象；
- 个人反馈优先级高于通用标签，但不能突破永久排除规则。

## 九、下一阶段边界

4.3.1 完成后，下一步才进入算法升级：

1. 先给首批核心歌曲加入结构化标签；
2. 增加旧 `tags` 到新 `profile` 的兼容读取；
3. 增加标签匹配分数；
4. 加入推荐解释生成；
5. 用固定样例比较升级前后的推荐结果；
6. 确认结果稳定后，再扩充歌曲库。

本阶段不做：

- AI 实时推荐；
- 自动抓取第三方歌单；
- 修改 room state；
- 修改音乐同步边界；
- 增加播放器、歌词或社区功能。

## 结论

标签体系的价值不是给歌曲增加更多文字，而是把“歌曲是什么”和“今天为什么适合”分开描述：

```text
歌曲标签
  ↓
匹配天气、时间、星期和个人偏好
  ↓
得到可解释的候选排序
  ↓
生成简短、真实的推荐理由
```

先建立可靠的小标签体系，再扩大歌曲库和升级算法，能比直接加入大量歌曲更有效地降低重复并提高推荐质量。
