# 音乐交互设计方案

## 设计定位

今日音乐不是播放器，也不是音乐社区，而是情侣工作台里的每日陪伴入口。

它要让用户感到：今天打开时，有一首适合现在的歌在等我；同时保留个人口味边界，不把个人音乐偏好塞进情侣房间同步。

## 一、当前音乐体验问题分析

### 1. 首页卡片缺少期待感

当前主要展示歌曲、歌手和来源，用户能知道“是什么歌”，但不容易知道“为什么今天推荐它”。音乐模块更像一个信息展示区，而不是每日陪伴内容。

### 2. 从歌单抽歌不等于推荐

单纯从已有歌单中随机抽取，只能保证歌曲来自熟悉范围，无法结合时间、天气、星期、最近已看歌曲和个人反馈形成选择依据。

### 3. 重复率会持续升高

歌曲库规模有限时，随机抽取容易反复出现同一批歌曲。即使有相似推荐，如果没有最近推荐历史、跳过记录和个人排除记录，也很难产生“发现新歌”的感觉。

### 4. 反馈入口不明确

用户目前缺少低成本的反馈方式。没有清晰的“喜欢”“换一首”“不再推荐”入口，系统就无法知道哪些推荐有效，也无法逐渐适应个人口味。

## 二、首页音乐卡片设计

### 推荐结构

首页默认只展示一首当前时段的推荐，避免增加首页信息密度：

```text
今日音乐
现在适合听

《歌曲名》
歌手

今天的天气 / 时间段 / 最近偏好，适合听一点……

查看今日三首  →
```

### 信息层级

1. 模块标题：今日音乐；
2. 当前时段：早晨、下午或夜晚；
3. 歌曲名：最醒目；
4. 歌手：次一级信息；
5. 一句推荐理由：最多两行；
6. “查看今日三首”：进入 Bottom Sheet；
7. “换一首”：只在卡片内作为轻量操作，不做大型按钮。

### 推荐理由

推荐理由应说明“今天为什么适合”，而不是声称系统理解了用户的私人情绪。

合适：

```text
今天下着雨，适合把节奏放慢一点。
```

不合适：

```text
系统知道你现在很难过，所以为你选择了这首歌。
```

理由最多两行，长文案在逗号处优先换行，避免撑高卡片。

## 三、今日三首音乐 Bottom Sheet

点击“查看今日三首”后，使用现有 Bottom Sheet，不新增导航页面。

### 页面结构

```text
今日三首
为今天的三个时段，准备了三种陪伴。

☀️ 早晨
歌曲名
歌手
封面
推荐理由

🌤 下午
歌曲名
歌手
封面
推荐理由

🌙 夜晚
歌曲名
歌手
封面
推荐理由
```

### 每首歌曲展示

应该展示：

- 歌曲名；
- 歌手；
- 小尺寸封面或现有音乐图标；
- 所属时段；
- 一句推荐理由；
- 音乐平台入口；
- “喜欢”“换一首”“不再推荐”等低干扰操作。

可以展示的推荐依据：

- 当前时段；
- 天气；
- 星期或周末状态；
- 最近反馈形成的风格倾向。

不应展示：

- 复杂分数；
- 推荐算法权重；
- 过度技术化的标签列表；
- 用户隐私性质的推断；
- “匹配度 98%”等虚假精确数字。

## 四、用户反馈机制设计

### 最终建议

V1 保留三种反馈：

1. 喜欢：表示以后可以更多推荐相近风格；
2. 换一首：表示今天不想听这一首，但不代表永久讨厌；
3. 不再推荐：明确永久排除，入口放在更多操作或次级位置，避免误触。

### 不建议加入

- 五星评分；
- 复杂心情打分；
- 评论；
- 社交点赞；
- 收藏夹体系；
- 排行榜；
- 连续命中奖励。

### 反馈语义

“换一首”和“不再推荐”必须明确区分：

- 换一首：只影响当前时段或当前推荐；
- 不再推荐：影响未来个人推荐，但允许用户在设置中恢复。

## 五、换一首逻辑设计

点击换一首后，不使用纯随机，而是重新从候选歌曲中计算排序。

流程：

```text
读取当前时间、天气、星期和个人反馈
↓
排除当前歌曲、近期已推荐歌曲和永久排除歌曲
↓
重新计算候选排序
↓
选择新的候选歌曲
↓
记录本次推荐和换歌行为
```

规则：

- 同一时段不能立即换回刚刚的歌曲；
- 换一首不改变原始歌单；
- 换一首不进入房间同步；
- 换一首不等同于不喜欢；
- 如果候选不足，允许较早歌曲重新出现，但不应连续重复。

## 六、推荐历史机制

### 建议记录

个人本地记录即可，不进入 room state：

```js
musicHistory: [
  {
    songKey: 'source:id-or-title',
    date: 'YYYY-MM-DD',
    daypart: 'morning',
    action: 'recommended',
    createdAt: 0
  }
]
```

### 行为类型

- `recommended`：系统展示过；
- `skipped`：用户点击换一首；
- `liked`：用户喜欢；
- `blocked`：用户永久排除。

### 保留策略

- 最近推荐记录保留约 30 天；
- 永久排除列表单独保留；
- 超过上限时优先清理普通推荐历史；
- 不因为清理历史而恢复永久排除歌曲。

## 七、个人推荐与情侣共享边界

### 个人数据，不进入房间同步

- 喜欢与不喜欢；
- 换一首记录；
- 永久排除歌曲；
- 最近推荐历史；
- 个人风格偏好；
- 当前设备的推荐缓存。

### 未来可以进入共享空间的数据

- 两个人共同选定的歌曲；
- 共同收藏的纪念日歌曲；
- 某次旅行或某段回忆关联的歌曲；
- 双方明确发送给彼此的歌曲；
- 共同歌单中的歌曲。

原则：

```text
系统推荐是个人体验
共同选择才是情侣空间数据
```

## 八、未来 AI 推荐预留

当前不接入 AI。未来可以由推荐模块调用 AI 生成候选理由或候选歌曲建议，但最终仍应经过本地规则和安全过滤。

### 可提供给 AI 的输入

- 双方公开歌单的风格摘要；
- 最近的喜欢、跳过和排除记录；
- 当前天气；
- 日期、星期和时段；
- 用户主动选择的心情标签。

### AI 可返回

- 候选歌曲；
- 歌手；
- 风格标签；
- 一句推荐理由。

### 必须保留的本地控制

- 过滤不存在或不完整的歌曲；
- 过滤永久排除歌曲；
- 过滤近期重复歌曲；
- 限制文案长度；
- AI 失败时回退到本地规则推荐；
- 不把 AI 原始结果直接写入房间同步。

## 九、第一版功能范围

### V1 做什么

- 首页展示当前时段的一首音乐；
- 点击进入今日三首 Bottom Sheet；
- 展示歌曲、歌手、来源和简短理由；
- 支持“喜欢”；
- 支持“换一首”；
- 支持“不再推荐”；
- 保留最近推荐和换歌历史；
- 按个人设备保存反馈；
- 继续使用现有歌曲库和规则推荐；
- 维持当前网易云与 Apple Music 的个人推荐边界。

### V1 不做什么

- 不接 AI 实时推荐；
- 不接第三方音乐 API；
- 不做自动播放；
- 不做播放器和歌词系统；
- 不做评论、社区和排行榜；
- 不把音乐偏好加入 room sync；
- 不做共同收藏页面；
- 不把推荐改造成任务或积分系统。

## 十、推荐后的用户体验流程

```text
打开首页
↓
看到当前时段的一首歌和一句理由
↓
感兴趣则点击歌曲或查看今日三首
↓
如果不合适，点击换一首
↓
如果喜欢，点击喜欢
↓
系统记录个人反馈，未来逐渐调整候选排序
```

整个过程不要求用户填写信息，也不要求用户完成任务。

## 十一、数据需求

### 当前即可复用

- `MUSIC_LIBRARY`：静态歌曲库；
- 当前时段、天气、星期计算；
- `_musicHistory`：最近推荐历史；
- `_musicLikes`：喜欢和不喜欢反馈；
- `_musicBlocked`：永久排除；
- `_musicSlotSongKeys`：当前时段缓存。

### 后续状态模块可能需要

- 统一的 `musicState` 读取接口；
- 推荐、跳过、喜欢、排除的事件记录；
- 历史清理和恢复排除歌曲的接口；
- 个人设置中查看和撤销“不再推荐”的入口。

这些数据继续属于本地个人状态，不进入房间同步。

## 十二、后续代码模块对应关系

```text
features/music/music-data.js
  静态歌曲、标签、歌词和平台链接

features/music/music-recommend.js
  时间、天气、星期、筛选、评分、排序和候选选择

未来 features/music/music-state.js
  喜欢、换歌、排除、历史和本地状态

未来 features/music/music-view.js
  首页卡片、今日三首 Bottom Sheet 和反馈按钮展示

app.js
  提供上下文、初始化模块、保持现有公开 API 兼容
```

## 结论

第一版音乐功能的重点不是增加更多歌曲，而是建立清晰反馈闭环：

```text
推荐
↓
用户轻量反馈
↓
记录个人历史
↓
下一次减少重复并改善选择
```

下一步进入 4.2.4 前，应先根据本方案确认是否保留“喜欢”、是否保留“永久排除”，再决定 `music-state.js` 的最小字段和接口。
