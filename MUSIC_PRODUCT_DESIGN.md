# MUSIC_PRODUCT_DESIGN

版本：2026-08-14
依据：MUSIC_ARCHITECTURE_AUDIT.md
范围：音乐产品逻辑、交互和后续模块化方案
本阶段不修改代码、不拆模块、不改变现有数据结构或同步行为。

## 1. 产品定位

今日音乐不是播放器，也不是音乐社区，而是首页里一个轻量的共同陪伴入口：

> 今天打开时，给你和 TA 各一首适合当下的歌，偶尔带来一点新的发现。

核心目标：

- 让两个人每天看到不同但有联系的音乐推荐。
- 减少只重复自己歌单内容的问题。
- 让推荐理由和当天生活情境有关。
- 保持点击成本低，不制造播放、收藏、打卡压力。
- 不影响现有同步、房间和其他生活功能。

## 2. 当前问题

现有音乐功能已经具备：

- 网易云、Apple Music、相似推荐三类来源。
- 按时间、天气、星期和标签选曲。
- 同一时段固定歌曲。
- 喜欢、换一首、不再推荐。
- 本地推荐历史和偏好。

但仍有这些问题：

1. 选择逻辑和展示逻辑都集中在 app.js。
2. 首页卡片和唱片浮窗是两套展示入口。
3. 推荐理由较弱，用户不容易理解为什么推荐。
4. “我”和“TA”的推荐关系表达不够清楚。
5. 本地偏好不会跨设备同步，当前这是既定边界，但界面需要避免让用户误以为双方已共享。
6. 相似推荐与歌单推荐的来源标签需要更准确。
7. 歌曲 id、歌手和外链需要继续校验，不能因为扩充推荐而引入错误信息。
8. 推荐历史只解决技术层重复，尚未形成清晰的产品规则。

## 3. 第一版产品方向

第一版不接外部音乐 API，不接 AI，不自动播放，不增加播放器。

首页保留双卡：

    今日音乐

    我 · 网易云
    歌曲
    歌手

    TA · Apple Music / 相似风格
    歌曲
    歌手

同时增加一个简短的共同情境说明：

    今天的天气和心情，适合听一点轻松的故事感音乐。

这里的“共同”只描述推荐情境，不暗示两端偏好已同步。

## 4. 推荐来源和权重

推荐结果由三部分组成：

### 4.1 双方已有歌单基础：约 30%

目的：保证熟悉感和命中率。

网易云侧只从：

- 你们的网易云歌单

Apple Music 侧优先从：

- 你们的 Apple Music 歌单

已有歌单歌曲具有更高的基础可信度，但不能每天重复同一批歌曲。

### 4.2 相似音乐扩展：约 50%

目的：让推荐有发现感。

相似推荐可以根据静态标签扩展：

- 歌手风格
- 语言
- 情绪
- 节奏
- 独立/流行/说唱等风格
- 已有歌曲的共同标签

相似推荐必须保留明确来源标签：

    相似推荐

不能把相似歌曲伪装成某一方歌单里的歌曲。

### 4.3 当天情境：约 20%

输入：

- 时段
- 星期
- 天气
- 季节
- 是否周末

推荐文案示例：

- 晴天：今天适合听一点轻松的歌。
- 下雨：雨声很轻，适合把节奏放慢。
- 周五晚上：这一周辛苦了，今晚可以轻松一点。
- 工作日下午：先陪你把这一段时间过完。
- 晚上：适合回家后和 TA 安静听一会儿。

这些文案只能使用真实可得的天气、时间和页面状态，不编造用户行为。

## 5. 推荐结果设计

当前仍使用现有歌曲对象，暂不写入新的 musicRecommendation state。

推荐结果在产品层建议统一成以下展示模型：

    {
      title,
      artist,
      source,
      url,
      id,
      slot,
      reason,
      mood,
      audience
    }

说明：

- title、artist、source、url、id 来自现有歌曲对象。
- slot 为 morning/noon/night。
- reason 是本地规则生成的短文案。
- mood 是展示用标签，不进入当前房间同步。
- audience 为 me/partner，用于界面标记，不等同用户身份数据。

第一版实现时可以只在内存中组合这个展示模型，不改变现有 state。

## 6. 首页布局

建议首页只保留一块简洁音乐模块，不再让唱片浮窗承担主要信息。

    今日音乐
    今天为你们各选了一首

    [我 · 网易云]
    歌名
    歌手
    一句推荐理由

    [TA · Apple Music]
    歌名
    歌手
    一句推荐理由

    查看今日三首 →

布局原则：

- 两张卡等宽。
- 歌名最多两行，超出时省略。
- 歌手单行显示。
- 推荐理由最多两行，并在逗号处优先换行。
- 不显示过多标签、歌词和平台按钮。
- 只保留一个“查看今日三首”入口。
- 首页不显示“自动换歌”或复杂操作。

## 7. 今日三首 Bottom Sheet

点击“查看今日三首”后打开现有 Bottom Sheet，不新增页面。

内容：

### 顶部

    今日三首
    给今天的你们，留三段不同的陪伴。

### 早晨

- 歌名
- 歌手
- 来源
- 一句原因

### 下午

- 歌名
- 歌手
- 来源
- 一句原因

### 夜晚

- 歌名
- 歌手
- 来源
- 一句原因

### 情境摘要

    今天的天气：晴 / 阴 / 雨
    今天是：星期几
    当前时段：早晨 / 下午 / 夜晚

暂不显示“你们喜欢的类型”这一类推断性文案，除非项目确实有稳定、可验证的偏好数据。

## 8. 换一首逻辑

“换一首”只作用于当前用户当前来源和当前时段。

例如：

- 我点击换一首，只改变我的网易云卡。
- TA 的 Apple Music 卡不变。
- 不改变对方设备的推荐。
- 不写房间同步。
- 不清空另一时段的推荐。

换歌流程：

    当前歌曲
      ↓
    写入当前 slot 的拒绝记录
      ↓
    排除当前歌曲、近期歌曲和永久屏蔽歌曲
      ↓
    重新评分
      ↓
    选择下一首
      ↓
    保存本地 slot key
      ↓
    只刷新当前音乐展示

如果没有合适候选：

    暂时没有更合适的一首，先保留这首。

不要强行随机到已经被永久排除的歌曲。

## 9. 喜欢、不喜欢和永久排除

### 喜欢

作用：

- 提高该歌曲未来再次被推荐的权重。
- 只保存到当前设备个人偏好。
- 不触发房间同步。

视觉：

- 使用轻量心形按钮。
- 点击后显示“记住了，之后会更常遇见类似的歌”。
- 可以再次点击取消喜欢。

### 换一首

作用：

- 只排除当前 slot 的当前歌曲。
- 不等于永久不喜欢。
- 后续日期仍可能再次出现。

### 永久不再推荐

作用：

- 单独入口，不放在最显眼的位置。
- 进入永久屏蔽列表。
- 需要轻量确认，避免误触。
- 只影响当前设备的个人推荐。

文案：

    之后少推荐这首歌？

确认后：

    已从你的推荐里移开。

不要使用惩罚、失败或负面评价文案。

## 10. 防重复机制

第一版建议使用三层防重复：

### 10.1 当前 slot 排除

当前时段换过的歌曲，当天当前时段不再返回。

### 10.2 近期历史排除

默认排除最近 7 天或最近 12 首，取两者较严格的结果。

### 10.3 长期历史软降权

最近 30 天出现过的歌曲降低评分，但在候选不足时允许再次出现。

这样比“30 天绝对不重复”更稳妥，因为静态歌曲库有限，过度限制会导致没有合适歌曲。

## 11. 每日三首的稳定性

同一天、同一设备、同一来源、同一时段默认固定。

稳定 key：

    日期 + 时段 + audience/source + 当前推荐版本

推荐版本用于以后调整算法时避免旧缓存永久阻塞新规则，但第一版不要求加入新字段。

以下行为不应导致无故换歌：

- 刷新页面
- 关闭再打开 PWA
- 打开或关闭音乐浮窗
- 重新进入首页
- 天气缓存重新渲染

以下行为可以换歌：

- 用户点击换一首
- 用户永久排除当前歌曲
- 进入新的时段
- 日期变化
- 明确升级推荐算法版本

## 12. 推荐理由规则

推荐理由必须短、具体、可解释。

模板示例：

- 今天晴天，适合听一点轻松的故事感。
- 下午节奏放慢一点，陪你把手头的事做完。
- 雨天适合留一点安静给自己。
- 周五晚上，选一首让人松下来的歌。
- 夜里不必太用力，听点温柔的。

避免：

- “系统认为你会喜欢”
- “根据你们的深层性格”
- “你们今天一定会发生什么”
- 没有数据依据的关系判断
- 过度 AI 化的长段落

## 13. 数据归属决策

第一版明确分成两类：

### 个人推荐数据

继续保存在本地，不进入 room sync：

- 喜欢
- 当前设备换一首
- 永久不推荐
- 本地推荐历史
- 本地时段 slot 缓存
- 本地音乐通知已读状态

### 未来共享数据

当前不实现，未来若需要再设计：

- 今天两个人共同选定的一首歌
- 共同收藏的歌曲
- 共同音乐回顾
- “今天一起听”的历史记录

不要把个人推荐状态直接加入 serializeRoom()，否则两台设备会因为个人操作互相覆盖。

## 14. 第一版暂不做

- 网易云 API
- Apple Music API
- Spotify API
- AI 实时推荐
- 自动播放
- 页面内播放器
- 歌词同步
- 播放统计
- 收藏歌单管理
- 音乐社区
- 音乐 Push
- 新导航 Tab
- 独立音乐页面

## 15. 模块化顺序

产品逻辑确认后，再按以下顺序拆：

### 15.1 music-data.js

迁移静态歌曲库、歌词、时段配置和平台链接。

要求：

- 不改变歌曲 key。
- 不改变 source 文案语义。
- 不改变现有对外歌曲字段。
- app.js 可以通过兼容变量继续读取。

### 15.2 music-recommend.js

迁移纯推荐逻辑：

- 时段 profile
- 天气 profile
- 星期 profile
- 标签评分
- 来源过滤
- 稳定 hash
- 防重复

不能直接操作 DOM、localStorage 或 Worker。

### 15.3 music-preferences.js

封装现有 _music* 字段：

- 初始化
- 读取
- 喜欢/取消喜欢
- 当前 slot 拒绝
- 永久屏蔽
- 推荐历史

内部字段暂不改名，避免破坏已有本地数据。

### 15.4 music-view.js

迁移：

- 音乐浮窗渲染
- 音乐卡片展示数据
- 反馈按钮
- 歌曲链接

life.js 只负责调用展示接口和打开 Sheet。

### 15.5 公共适配接口

保留：

    window.PufferLife.getDailyMusic()

未来可增加，但不替换现有接口：

    window.PufferMusic.getToday()
    window.PufferMusic.changeCurrent()
    window.PufferMusic.like()
    window.PufferMusic.block()

## 16. 验收标准

### 功能

- 首页仍显示两方不同来源的推荐。
- 网易云推荐不出现 Apple Music 歌曲。
- 相似推荐来源正确标注。
- 刷新不会无故换歌。
- 换一首只影响当前一方和当前时段。
- 喜欢可取消。
- 永久排除不会被重新推荐。
- 没有候选时页面仍正常显示。
- 音乐失败不会影响同步和首页其他模块。

### 数据

- 现有 _music* 字段仍可读取。
- 不新增 room state 字段。
- 不影响留言、照片、旅行、喝水和默契同步。
- 不改变歌曲 key 的稳定性。

### 视觉

- 首页不增加明显高度。
- Bottom Sheet 使用现有 iOS 风格。
- 长文案在逗号处优先换行。
- 歌名、歌手、来源在 iPhone 14 Pro 宽度下不溢出。
- PC 端保持居中且不出现重复音乐入口。

## 17. 最终产品判断

音乐第一版应该先成为一个“每天有一点新鲜感的陪伴模块”，而不是复杂播放器。

推荐路线：

    现状审计 ✅
      ↓
    产品方案 ✅
      ↓
    歌曲数据校验
      ↓
    纯数据拆分
      ↓
    推荐逻辑拆分
      ↓
    本地偏好适配
      ↓
    首页和 Bottom Sheet 视觉升级
      ↓
    真实双端体验
      ↓
    再决定是否加入 sharedMusic

当前最重要的设计决策是：

> 个人推荐保持个人，只有“共同选定/共同收藏”的音乐才进入未来共享状态。

本文件没有修改任何 JS、CSS、Worker 或数据结构。
