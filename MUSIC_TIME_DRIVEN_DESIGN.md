# 时间驱动型音乐体验方案

版本：4.4.1.5  
状态：设计稿，暂不实现

## 一、为什么调整固定三首方案

原方案会在一天开始时同时展示早晨、下午和晚上三首歌，但未来时段的实际天气、双方状态和使用场景都还没有发生，因此会产生几个问题：

- 下午和晚上的天气可能与早上不同；
- 用户的心情和当天经历会变化；
- 推荐理由可能在真正到达时段前已经失效；
- 用户提前看到所有结果，减少了每天回来查看的期待感；
- “今日三首”更像静态歌单，不像陪伴型推荐。

因此，音乐推荐调整为：

> 只生成当前时段真正需要的一首歌，时间变化后再生成下一首。

## 二、时间驱动型方案

系统根据当前时间判断时段，并结合此刻可获得的上下文生成推荐：

```text
当前时间
天气
星期
已有音乐偏好
近期推荐历史
        ↓
当前时段推荐
        ↓
歌曲 + 理由
```

每个时段只生成一次当前推荐。刷新页面不会重复生成，进入下一个时段后才允许生成新的推荐。

## 三、时段定义

```text
morning    07:00–11:59
afternoon  12:00–17:59
night      18:00–次日 06:59
```

兼容现有代码中的 `noon`：

- 对外新接口使用 `afternoon`；
- 内部仍可暂时识别 `noon`；
- 迁移期间不删除旧时段字段，避免旧缓存失效。

## 四、当前时段推荐流程

1. 读取本地当前日期和时段；
2. 读取当前天气和星期信息；
3. 读取个人音乐历史、喜欢和排除记录；
4. 检查当前时段是否已有缓存；
5. 有缓存时直接返回，不重新抽歌；
6. 没有缓存时调用推荐引擎生成一首；
7. 使用推荐理由模块生成结构化理由；
8. 保存当前时段结果和生成时间；
9. 下一时段开始后，生成新的推荐并追加历史。

## 五、首页音乐卡片

首页不再展示三首未来歌曲，只展示此刻推荐：

```text
今日音乐

此刻推荐

《歌曲名》
歌手

今天的天气和时间，适合听一点轻松的歌。

查看详情 →
```

首页只负责：

- 告诉用户当前推荐是什么；
- 展示一条自然理由；
- 引导进入详情；
- 不提前展示未来时段内容。

卡片保持现有 iOS 原生信息层级，不做播放器进度条、自动播放或大型唱片视觉。

## 六、详情 Bottom Sheet

标题从“今日三首”调整为：

```text
今日音乐
```

内容结构：

```text
此刻推荐
歌曲
歌手
推荐理由

今天听过
上午    ✓ 已生成
下午    12 点后解锁
晚上    18 点后解锁
```

已经过去的时段展示实际听过或推荐过的歌曲；未到达的时段只显示解锁时间，不提前生成歌曲。

当前时段的反馈按钮放在歌曲信息下方：

- 喜欢；
- 换一首；
- 今天不太适合。

不新增独立页面，继续使用现有 Bottom Sheet。

## 七、music-state.js 数据设计

未来建议在个人本地状态中维护：

```js
todayMusic: {
  date: '2026-08-14',
  current: {
    slot: 'morning',
    songKey: '',
    reason: {},
    generatedAt: 0
  },
  history: [
    {
      date: '2026-08-14',
      slot: 'morning',
      songKey: '',
      reason: {},
      generatedAt: 0
    }
  ]
}
```

设计原则：

- `date + slot` 是当前时段缓存的稳定键；
- `songKey` 优先保存歌曲键，不重复保存完整歌曲对象；
- `reason` 可以缓存，也可以根据歌曲和上下文重新生成；
- 历史属于个人推荐状态，不进入情侣房间同步；
- 第二天进入时自动创建新的日期记录；
- 不覆盖现有 `_musicHistory` 等旧字段，迁移时兼容读取。

## 八、music-recommend.js 接口调整

建议未来增加：

```js
getCurrentMusic()
```

返回：

```js
{
  slot: 'night',
  song: {},
  reason: {},
  generatedAt: 0,
  cached: false
}
```

职责：

- 根据当前时间计算时段；
- 获取当前可用天气、星期和个人状态；
- 读取或生成当前时段歌曲；
- 不一次生成未来时段；
- 不改变现有 `pickMusicFor()` 的底层评分能力；
- 兼容现有 `getMusicSlotSong()`，逐步迁移调用方。

## 九、推荐理由模块调整

`music-reason.js` 继续使用规则模板，但输入应明确为当前时段：

```js
generate(song, {
  part: 'night',
  weather: {},
  preferenceTags: []
})
```

文案重点：

- 早晨：开启一天、慢慢清醒；
- 下午：工作间隙、保持节奏；
- 夜晚：一天结束、放松和陪伴；
- 晴天：明亮、轻快；
- 雨天：安静、慢下来、陪伴。

不要生成“未来晚上会怎样”的推测理由，只解释当前实际推荐。

## 十、用户反馈机制

反馈只作用于当前时段和未来个人推荐：

- 喜欢：提高类似风格的权重；
- 换一首：记录当前歌曲已被跳过，并在当前时段重新选择；
- 今天不太适合：降低当日相似歌曲权重；
- 不再推荐：沿用现有永久排除机制。

反馈不改变：

- 房间共享数据；
- 对方的推荐结果；
- 另一方的音乐偏好；
- 当前歌曲的历史记录。

## 十一、V1 实现范围

### 实现

- 当前时段判断；
- 当前歌曲缓存；
- 时段切换后重新生成；
- 当前推荐详情 Bottom Sheet；
- 当天已生成时段的历史展示；
- 现有反馈按钮接入当前歌曲；
- 推荐理由跟随当前上下文生成。

### 暂不实现

- AI 实时推荐；
- 第三方音乐 API；
- 自动播放；
- 歌词同步；
- 未来歌曲预生成；
- 共享音乐状态；
- 新导航 Tab；
- 播放统计、评论和复杂收藏系统。

## 十二、建议实现顺序

```text
4.4.2 个人音乐状态适配
        ↓
4.4.3 当前推荐接口
        ↓
4.4.4 首页当前音乐卡片
        ↓
4.4.5 当前音乐详情 Bottom Sheet
        ↓
4.4.6 喜欢 / 换一首 / 不适合今天
        ↓
手机与桌面回归测试
```

在实现完成前，不改变现有推荐算法、歌曲标签和房间同步契约。
