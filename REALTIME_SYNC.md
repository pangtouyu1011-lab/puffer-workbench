# 消息同步优化

基于 `codex/reaction-feedback-reliability` 的 `d41814e`。
已发布的前端版本：`20260907-realtime-sync-3`（2026-09-07）。

## 使用效果

- 文字和图片留言保存后立即进入上传队列，不再等普通编辑的 1 秒防抖。
- 前台设备在房间更新提交成功时收到提示，立即读取最新数据，不再等待下一个 3 秒轮询周期。
- 失去连接后仍保留本机消息和重试；回到前台、恢复网络时重新订阅。
- 前台在线时上传失败持续退避重试，最大间隔 120 秒，不再尝试六次后停止。隐藏或离线时暂停重试计时，恢复网络、显示页面或窗口重新获得焦点时立即补发待上传内容，重复恢复事件复用同一上传任务。
- 接收提示本身不会把消息标记为已同步；仍以成功上传的快照确认具体消息。
- 发送成功保存到本机后保留聊天窗口，直接查看每条留言的同步状态；清空已发送输入并保留后续草稿追踪，图片上传中继续输入的新草稿不会被清空。同步未确认时不再提示“已经送到”，保存结束后清除处理中提示并刷新字数。
- 图片压缩、图片上传及真实网络耗时仍然存在。后台系统通知沿用原 Web Push，不保证锁屏后实时送达。

## 实现与边界

`POST /api/v1/rooms/:roomId/changes`，JSON 请求包含 `pass` 和 `since`，口令不放入此接口的 URL。

房间 Durable Object 验证口令和版本。已有新版本时立即返回，否则最多等待 20 秒，返回 `{ok, rev, updatedAt}`；不返回口令或消息正文。每房间最多 16 个等待请求，超额返回 429。请求取消或超时会清理监听器；取消信号若未被运行环境传递，20 秒上限仍然有效。

版本检查和监听器注册之间没有异步间隔。成功提交事务后才通知，冲突、鉴权失败及其他房间更新不产生通知。这个只读接口不创建或迁移房间；原 GET/PUT 继续负责旧房间迁移。

浏览器每次只维持一个监听请求，25 秒客户端超时，失败后 3–30 秒退避；旧服务端 404/405 等情况一分钟后重试新通道，原 3 秒检查持续可用。正常监听期间减少整份快照读取，监听超过 30 秒未成功响应时恢复轮询。

所有提示均按已应用的 revision 重新监听；拉取失败不会跳过通知对应的版本。上传等待已开始的拉取结束，上传期间拉取复用上传任务；每份上传快照固定自己的 baseRev，保留原版本冲突合并重试和软删除规则。失败的上传队列等待退避，不立即无限排队重试。

退出房间、切换身份、进入后台、关闭页面时停止监听；恢复网络和重新显示页面时重连。无需数据库迁移，不更改既有房间 ID、口令或存储结构。

正常空闲前台每台约每 20 秒续接一个轻量请求。长轮询会保持 Durable Object 请求活跃，仍有运行费用；如将来扩展大量房间，可评估支持休眠的 WebSocket 通道。

## 验证

在 `shared-room-worker` 运行：

```sh
npm ci
npm run test:realtime
npm test
```

`test:realtime` 要求支持 `node:sqlite` 的 Node.js 22.13+。它运行实际前端同步函数、Worker 路由和真实 SQLite 事务，外部 KV 使用内存替身；不能代替 Cloudflare 运行环境及手机真机验证。

本次本机验证：21 项行为测试通过，包含并发发送、上传中新增、接收提示、拉取失败重连、鉴权、超时、房间隔离、停止监听、旧服务端回退和旧快照保护，以及发送时间保留、断网补发保留原始时间、超过六次失败继续重试、后台暂停和重复恢复事件不重复上传。另覆盖发送后保留聊天窗口、失败及慢上传保护新草稿、照片预览资源释放与反馈清理；在 Miniflare 测试集中加入 4 项服务端用例。

本机 workerd 启动发生 Windows 原生 access violation，因此 Miniflare 集成测试未能运行。非 workerd 测试共 85 项（含上述 21 项），81 项通过，4 项界面源码匹配断言在原分支 HEAD 上也失败：`frontend-room-sync.test.cjs` 的草稿清理位置、弹层 fixed 样式，以及 `offline-shell.test.cjs` 的陪伴反馈调用形式、弹层背景样式。这些四项原有失败未修改。另已把通用保存提示的调用次数断言改为实际提示行为验证，以适配留言独立的同步状态提示。

## 发布与验收

1. 在可运行 workerd 的环境完成 `npm test`，区分上面四项基线失败与本次改动，并单独运行 `npm run test:realtime`。
2. 登录具有此项目权限的 Cloudflare 账号，先部署 `shared-room-worker`，保持生产 Durable Object/KV/D1/R2 绑定不变。
3. 将前端运行白名单文件部署到 Pages 项目 `pufferwork`，不上传源码说明、测试、工具、令牌或 `.git`。
4. 确认 `version.json` 为 `20260907-realtime-sync-3`，两台设备更新 PWA。
5. 在测试房间验证两端同时发送、连续发送、断网后恢复及切前后台；检查双方消息 ID 不重复、记录不丢失，测量真实网络下的耗时。

旧前端兼容新后端；新前端在旧后端自动回退。回滚可以先恢复之前的前端，再恢复 Worker，房间数据无需回滚。

2026-09-07 已获得发布授权并完成 Worker 和 Pages 正式部署。Worker 版本 `1b8e6da2-77ae-4e55-88a7-c149e361217d`，Pages 部署 `0b685b62.pufferwork.pages.dev`。原 Worker 版本 `54111e48-b172-4a6b-b927-54f46c82a13e`，原 Pages 部署 `79280dc2-42f4-462d-a196-a921128c7fd0`，保留供回滚参考。修正 Wrangler 中 routes 的 TOML 层级，显式维持原正式域名路由；数据库、存储绑定和定时任务不变。

发布后核对正式域名的 version.json、index.html、app.js、life.js 和 service-worker.js，内容哈希均与发布文件一致；Worker /health 正常，新 /changes 接口对缺少口令的请求返回 400 pass_required。该检查不创建房间、不发送留言，不能替代双 iPhone 真机同步验收；本机 workerd 集成测试的启动崩溃仍未解决。

2026-09-07 使用已登录控制台只读核对了 D1 留言镜像的时间字段，四条北京时间 09:00 的记录其时间戳与编号中的创建时间一致；用户确认它们是异常留言，发送端为 iPhone 主屏幕网站。这一发现不能证明实际发送时刻，也不能代替权威快照或发送设备核对。尚未确定历史留言时间异常的原因，未改写任何历史时间。
