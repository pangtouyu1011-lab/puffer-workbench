# Puffer Workbench · DeepSeek V4 Flash 交接文档

更新时间：2026-08-10

## 0. 任务目标

这是一个两人共同生活记录网站，重点是让两个人每天自然打开：看看对方状态、完成一点轻互动、记录小事、回看共同生活。

- 线上地址：`https://20051011.xyz`
- 前端仓库：`https://github.com/pangtouyu1011-lab/puffer-workbench`
- 当前工作分支：`codex/prevent-empty-room`
- Cloudflare Pages 项目：`pufferwork`
- 同步 API：`https://sync.20051011.xyz`

请全程使用中文和非技术语言向用户说明结果；不要要求用户提供或粘贴令牌、口令、数据库密码。

## 1. 当前生产架构（真实状态）

```text
手机 / 浏览器
  ↓
Cloudflare Pages（20051011.xyz，静态前端）
  ↓
Cloudflare Worker（sync.20051011.xyz）
  ├─ KV：当前房间完整同步数据的真实来源
  └─ D1：房间同步索引（已接入，尚未承载完整业务数据）
```

### 数据原则

1. `localStorage` 是本机即时保存来源；加入房间后，前端会把合并后的房间状态推送给 Worker。
2. Worker 内的 Cloudflare KV（绑定名 `BENCH`）仍是跨设备同步的主来源，**不要在未经迁移设计与验证的情况下改成 D1 读写**。
3. D1 数据库 `pufferwork-db` 已创建并绑定到 Worker。每次成功 PUT 房间状态后，Worker 会尽力更新 `room_sync_index`（房间 ID、版本号、更新时间、后端标记）；D1 写入失败不会中断 KV 同步。
4. R2 尚未开通，照片仍采用现有压缩数据随房间同步的兼容方式。**不要保留、发布或引用 R2 绑定/上传代码**，直到用户完成 R2 开通。
5. 绝不记录房间口令、访问令牌或用户私密数据到日志、D1 索引、Git 或交接文档。

## 2. 关键文件

| 文件 | 用途 |
|---|---|
| `index.html` | 主页面、资源版本号和 PWA 刷新逻辑 |
| `app.js` | 原有业务数据、localStorage、房间同步、待办/照片/留言/音乐等 |
| `life.js` | 新 iOS 风格四页界面和交互层 |
| `life-unified-preview.css` | 当前统一的可爱 iOS 视觉样式 |
| `life-complete-state.js` | “双方当天互动全完成”庆祝态增强逻辑 |
| `life-complete-state.css` | 庆祝态样式 |
| `assets/status-pet-complete.png` | 已完成互动时使用的透明庆祝胖头鱼 |
| `scripts/bump-version.mjs` | 一键同步更新页面版本、资源版本参数与 `version.json` |
| `shared-room-worker/worker.js` | 线上同步 Worker（ES Module 格式） |
| `shared-room-worker/wrangler.toml` | Worker、KV、D1 绑定配置 |
| `shared-room-worker/migrations/0001_room_sync_index.sql` | 已执行的 D1 索引表结构 |
| `AGENTS.md` | 项目规则与历史说明；其中“Pages 为 Direct Upload”的描述已过期，以实际 Pages GitHub 自动部署为准 |

## 3. 当前已完成的功能

### 首页与互动

- 顶部天气与天气胖头鱼。
- 今天最重要的待办。
- 两人当天互动：各自抽签、选择心情、留言、完成待办（无当天待办视为完成）。
- 对方当天动态、双平台今日音乐、双人运势、抽签入口。
- 当两人 8 个互动都完成时，“今天的互动”卡会自动替换为庆祝胖头鱼，文案为：
  - `今天被你们好好过完了`
  - `每个小互动，都收到了彼此的回应。`
- 未完成时仍保持原本可点击的互动按钮；次日自动按当天数据重新判断。

### 页面能力

- 四个主页面：今天、日子、小事、我们。
- 待办、日历、训练记录、共同照片、留言、心愿、设置等已有页面入口。
- 留言支持发送图片；照片/留言等仍使用现有房间同步逻辑。
- 底部导航、Bottom Sheet 与整体 iOS 风格已做过多轮视觉统一。
- 图片上传会自适应压缩，单张嵌入图片上限为 500 KB；接近浏览器或同步容量上限时会提示备份/清理，达到硬上限时阻止保存，避免静默同步失败。

### 同步与发布

- Worker 已部署且健康检查返回：`{ ok: true, storage: { kv: true, d1: true } }`。
- D1 的 `room_sync_index` 与更新时间索引已创建。
- 最近生产发布：提交 `36e93b1`，线上资源版本 `20260810-capacity-guard-1`，已确认线上 `version.json` 和首页资源均已生效。
- Cloudflare Pages 当前确实由 GitHub 推送自动触发生产部署，生产分支就是 `codex/prevent-empty-room`。不要按 `AGENTS.md` 中“Direct Upload”的旧说明操作。

## 4. 近期提交

| 提交 | 内容 |
|---|---|
| `9ee1c83` | 全完成互动时显示庆祝胖头鱼，新增素材与缓存版本 |
| `36e93b1` | 图片容量保护、存储使用提示、版本号一键更新脚本 |
| `718c07a` | Worker 转 ES Module，绑定 D1，新增房间同步索引 |
| `14a8d46` | 清理音乐区域残余边框 |
| `1bcb846` | 修复“我们”页设置入口 |
| `9c7db73` | 统一可爱 iOS 视觉 |

## 5. 接下来建议的优先级

### P0：每次改动必做

1. 仅改动用户明确要求的功能，不顺手重构数据层。
2. 运行 `node --check app.js` 与 `node --check life.js`（以及新增 JS）。
3. 用手机宽度检查首页、日子、小事、我们及所有 Bottom Sheet；尤其检查底部导航是否重复、弹窗是否能关闭、旧页面是否被误打开。
4. 前端发布前同步更新：
   - `version.json`
   - `index.html` 的 `pageVersion`
   - 改动资源的 `?v=` 查询版本号
   - 优先执行 `node scripts/bump-version.mjs <新版本号>`，不要手改三处后遗漏。
5. GitHub 推送后等待 Cloudflare Pages 成功部署，确认部署对应的是本次 commit，再清理 `20051011.xyz` 的 Cloudflare 缓存。
6. 不要把 QA 截图、浏览器临时目录、Supabase 遗留文件或任何秘密一起提交。

### P1：可继续做的产品工作

- 根据用户反馈继续压缩首页信息密度，保持“照片/天气优先、互动清晰、音乐简洁”。
- 对所有独立页与 Bottom Sheet 做统一视觉和关闭/拖拽体验检查。
- 优化“我们”页与训练、待办、留言等已存在能力，但不能丢失原有功能。
- 若用户开通 R2：再单独实现“Worker 受控上传与读取 + R2 私有桶”，保留旧照片兼容回退，并做真实上传/读取/删除测试后再发布。

### P2：数据层演进（不要急于执行）

- D1 后续可把待办、留言、训练、回忆拆为关系表；先新增 Worker API，再双写/校验，再逐步切读，最后才迁移历史数据。
- 当前 D1 只负责同步索引；不要把一整份房间 JSON 直接塞进 D1 当“迁移完成”。
- R2 开通后，图片二进制放 R2，业务数据仅存图片 URL；仍由 Worker API 提供访问，不让前端直连存储。

## 6. 重要风险与禁区

1. **同步不能回退为 Supabase 或 `*.workers.dev`**：当前稳定方案是自定义域名 `sync.20051011.xyz` + Worker KV。
2. **不要清空、覆盖或批量迁移 KV 房间数据**。用户过去有过历史数据丢失/空房间问题，对数据安全非常敏感。
3. `life.js` 是历史迭代叠加较多的单文件，存在后定义覆盖前定义的情况。修改首页时先定位最终生效的 `lifeHomeDashboard`，不要仅编辑前面同名函数。
4. 以下 Supabase 文件是本机遗留实验，不属于当前 GitHub/生产架构；不要提交，也不要覆盖用户本机内容：
   - `supabase/config.toml`
   - `supabase/rooms.sql`
   - `supabase/functions/room-presence/`
   - `supabase/sql/room-presence/`
5. 不要提交 QA 文件（如 `*-qa.png`、`.qa-chrome-profile/`）或未知的未跟踪文件。
6. 不要为了“清理工作区”使用 `reset --hard` 或覆盖用户文件；先用 `git diff` 确认内容，再只处理本次明确需要的文件。

## 7. 验证与发布参考

### 本地基础检查

```powershell
node --check app.js
node --check life.js
node --check life-complete-state.js
git diff --check
```

### Worker 基础检查

```powershell
cd shared-room-worker
.\node_modules\.bin\wrangler.cmd deploy --dry-run
.\node_modules\.bin\wrangler.cmd deploy
```

线上健康地址：`https://sync.20051011.xyz/health`

期待健康响应包含：

```json
{ "ok": true, "storage": { "kv": true, "d1": true } }
```

### 发布后的最低确认

1. Cloudflare Pages 最新生产部署状态为 success，commit hash 与本次提交一致。
2. `https://20051011.xyz/version.json` 返回新增版本号。
3. 首页 HTML 引用了本次新增或修改资源的版本号。
4. 手机端硬刷新后，不出现双底部导航、旧首页闪现、卡片错位或弹窗无法关闭。

## 8. 与用户协作风格

- 用户高度在意“真实可用”和手机端视觉，不接受只改代码但没检查上线结果。
- 先给简短进度，再执行；涉及发布必须明确说明已验证的证据。
- 遇到不可逆动作、迁移数据或需要付款/授权时，先解释影响并让用户决定。
- 视觉基调：iOS 原生感为主，暖色生活感为辅，胖头鱼只在有意义的节点出现；不要变成手账、游戏或堆满 emoji 的界面。

## 9. 视觉规范手册（必须遵守）

### 9.1 视觉目标

产品是“可爱但克制”的双人生活记录工具，而不是情侣手账、游戏或功能仪表盘。视觉配比：

- 约 90% iOS 原生体验：清晰层级、系统留白、低噪声、自然动效。
- 约 7% 温暖生活感：杏橙、奶油白、浅暖灰。
- 约 3% 胖头鱼 IP：只用于陪伴、完成反馈、空状态和当前导航，不占据内容主体。

### 9.2 版式与设备边界

1. **移动端优先**：以 iPhone 14 Pro 宽度（393 CSS px）作为首要设计基准；左右页面内边距保持一致，建议 16–20 px。
2. 内容区应使用单列自然流布局；文本、卡片标题、操作区的左右边界必须对齐。禁止用固定像素高度截断长内容。
3. 桌面端仅做居中、最大宽度约束和舒适留白，不能把移动端内容放大到溢出、图片裁切或出现横向滚动。
4. 底部导航只允许存在一套，使用安全区留白；任何独立页、弹窗、旧页面跳转都不得重复挂载导航。
5. 首页的信息顺序以“照片与天气 → 今日互动/待办 → 对方当日动态 → 音乐/轻陪伴”为准；当天无数据时宁可收起模块，也不要用虚构内容填满。

### 9.3 色彩、字体与层级

- 页面底色：iOS 分组浅灰/微暖灰；卡片以白色或极浅奶油白为主。
- 主强调色：温和杏橙；完成、提醒、选中状态使用同一套低饱和色阶。不要使用高饱和紫色、荧光粉色背景或大面积渐变。
- 正文使用系统字体优先级：`-apple-system, BlinkMacSystemFont, "SF Pro Text", "PingFang SC", "Microsoft YaHei UI", sans-serif`。
- 标题强调层级，而非字体花样：页面标题约 32 px，分组标题约 22–24 px，卡片标题约 17–18 px，正文约 15–16 px，辅助信息约 13–14 px。
- 主文字接近黑色，次级文字使用中性灰；不要用蓝色当作普通标题文字，蓝色只保留给明确可点击链接或系统语义。

### 9.4 卡片、按钮与图标

1. 卡片使用轻圆角、细边界和极弱阴影；避免厚重投影、胶带、纸张纹理或过多装饰边框。
2. 文字按钮（如“记录”“展开”“查看全部”）统一采用圆润胶囊外框，点击区域不小于 44×44 px。
3. 图标统一使用 Phosphor 图标库；同一页面必须统一描边粗细和尺寸。仅当前选中的底部导航项可使用胖头鱼图标，其他项使用简洁线性图标。
4. 对危险操作采用明确文字与确认，不用只有图标的隐蔽入口。
5. 加载、空状态、完成状态优先给出简短清楚文案；不允许把真实数据缺失伪装成已有内容。

### 9.5 胖头鱼 IP 规则

1. 原图是唯一角色基准：圆润轮廓、橙色/奶油色、表情、比例和像素风不得被重新绘制成其他风格。
2. 天气角色必须使用完整合成资产（例如 `weather-sunny-pet.png`），角色与墨镜/雨伞/云朵同层移动；禁止 CSS 叠加小物件造成错位。
3. 状态记录角色与天气角色分开设计，必须透明背景、完整抠图；不得出现紫色或洋红色方形底图。
4. 角色可在顶部天气、状态卡角落、底部当前导航、空状态、全完成庆祝等节点出现；同一屏通常不超过 2 个。
5. 动效只做轻微跳动、爱心浮出、淡入等，遵守 `prefers-reduced-motion`；不能遮挡文本和可点控件。

### 9.6 Bottom Sheet / 独立页规范

1. Sheet 使用 iOS 式底部弹层：背景遮罩、圆角顶部、居中拖拽条、右上角关闭按钮。
2. 拖拽条必须可实际拖动/下滑关闭；点击遮罩与关闭按钮也必须关闭。内部内容独立滚动，不能把底部导航或 Safari 工具栏挤乱。
3. 打开留言页时默认滚动到最新消息和输入框；点击“说句话”只打开对话，不混入“今天发生了什么”的记录流。
4. 日历必须展示月份与前后切换；训练按日期聚合；抽签页展示双方当天结果并支持未抽时一键抽签。
5. 每次新增/修改独立页，都要在手机宽度下检查：关闭、返回、滚动、底部安全区、图片加载、长文本与无数据状态。

### 9.7 动效与背景

- 背景只允许极轻的原生感层次（柔和光晕、低对比小气泡/表情缓慢上浮）；不得影响文字对比度、阅读或性能。
- 动效时长建议 180–320 ms，使用缓出曲线；页面切换和 Sheet 过渡应自然但不拖沓。
- 禁止持续高频旋转、强闪烁、大面积粒子、明显游戏化进度条。

### 9.8 视觉验收清单

发布前至少逐项确认：

1. 手机与桌面不横向溢出、不出现放大的像素角色、重复导航或旧页面闪现。
2. 全部按钮和卡片文字对齐，长文本可换行，图片保持正确比例且无彩色底块。
3. 当前页底部导航正确高亮，其他图标为 Phosphor 线性图标。
4. 所有 Sheet 可拖动、可关闭，内容多时可滚动，Safari 安全区正常。
5. 真实房间数据为空、很长、含图片、两人未同步时都不报错、不编造数据。
